# 카카오 디벨로퍼스 — Ra-Review 설정 참고서

콘솔 UI가 예전과 달라져 매번 헤매서 만든 문서.
**"어느 메뉴가 무엇을 담당하는가"** 를 확정해 두는 게 목적이다.

- 콘솔: <https://developers.kakao.com/console/app/1491338>
- 앱 ID `1491338` · 앱 이름 **Ra-Review** (구 라봉후기) · **비즈 앱**
- 앱 이름은 **공유카드 출처 줄 + 로그인 동의 화면**에 그대로 노출된다

---

## 🚨 도메인 설정이 세 군데다 (오늘 하루를 태운 함정)

이름이 비슷해서 헷갈리지만 **하는 일이 전부 다르다.** 하나만 고치면 반드시 어딘가 깨진다.

| # | 메뉴 경로 | 담당 | 안 맞으면 생기는 일 |
|---|---|---|---|
| 1 | 앱 설정 > **일반** > 앱 기본 정보 > **앱 대표 도메인** | 앱 정보 표시 | 표시만 이상함 (동작엔 영향 없음) |
| 2 | 앱 설정 > **플랫폼 키** > JavaScript 키 > **JavaScript SDK 도메인** | **JS SDK 실행 허용** | 그 페이지에서 `Kakao.*` 자체가 안 돎 → 로그인·공유 버튼 무반응 |
| 3 | 앱 설정 > **제품 링크 관리** > **웹 도메인** | **카톡 공유·메시지의 링크 이동 허용** | 🔥 **카드 링크가 "기본 웹 도메인"으로 갈아치워진다** |

### 3번이 진짜 범인이었다 (2026-08-04)

도메인을 `ra-review.com` 으로 바꾼 뒤, 카톡 공유카드를 누르면
`https://kmeimei2017-max.github.io/` (경로·쿼리 없는 **루트**)로 떨어져 404가 났다.
1번과 2번을 다 고쳤는데도 계속 재현됐고, **3번이 옛 도메인 그대로**였던 게 원인이다.

카카오 안내문이 그대로 설명해 준다:

> - 링크 이동을 허용할 웹 도메인 정보를 추가하고, **기본 웹 도메인**을 선택할 수 있습니다. 최대 10개.
> - **도메인 입력 시 경로를 포함한 경우, 경로는 제외되고 도메인만 등록됩니다.**
> - 기본 웹 도메인은 최초로 등록한 도메인이 설정되며, 다른 도메인을 추가해 변경할 수 있습니다.
> - **기본 웹 도메인은 카카오 제품의 웹 링크 기본 값으로 적용됩니다.**
> - Kakao SDK for JavaScript용 도메인은 [플랫폼 키] > [JavaScript 키]에서 등록합니다. ← **2번과 별개라고 못박음**

즉 이렇게 굴러갔다:

```text
카드 링크  https://ra-review.com/?view=<토큰>
              ↓ 3번 목록에 없음 → 이동 거부
기본 웹 도메인으로 대체  https://kmeimei2017-max.github.io
              ↓ 경로가 제외돼 저장되므로 "루트"다
결과  https://kmeimei2017-max.github.io/   → GitHub Pages 404
```

**우리 코드는 그런 URL을 만든 적이 없다.** 경로도 쿼리도 없는 주소가 나오면
= 카카오가 대체한 것 = **3번을 의심하면 된다.**

### 도메인을 바꿀 때 체크리스트

1. 1번 앱 대표 도메인 → 새 도메인
2. 2번 JS SDK 도메인 → 새 도메인 **추가**, **첫 줄에 오게** (미등록 시 첫 도메인으로 대체되는 동작이 있다)
3. **3번 웹 도메인 → 새 도메인 추가 + `기본`으로 지정** ← 빼먹기 쉬움
4. 옛 도메인은 지워도 된다. GitHub Pages가 새 도메인으로 301 리다이렉트해 주므로
   옛 링크로 들어와도 최종 화면은 새 도메인이다
5. **카톡 앱을 완전 종료 후 재실행** (설정 캐시)
6. 앱을 `?v=숫자` 로 새로 열어 카드를 **새로 만들어** 시험

### ⚠️ 카드는 "보낸 시점"의 설정이 박힌다

이미 보낸 카드는 설정을 고쳐도 **안 고쳐진다.** 새로 만든 카드부터 적용된다.
그래서 시험할 때는 **방금 도착한 카드만** 눌러야 한다. 채팅방에 쌓인 옛 카드로 판단하면 오진한다.

---

## 키 3종 (앱 설정 > 플랫폼 키)

| 키 | 어디서 쓰나 |
|---|---|
| **REST API 키** | Supabase가 카카오 OAuth 할 때의 `client_id`. 우리 코드엔 없다 |
| **JavaScript 키** | `index.html` 의 `Kakao.init()`. 공개 값이라 노출돼도 정상 |
| 네이티브 앱 키 | 안 씀 (앱 없음) |
| 어드민 키 | 🚨 **절대 코드·문서에 넣지 말 것.** 서버 전용 |

---

## 로그인 흐름 — 카카오는 Supabase까지만 안다

```text
앱(ra-review.com)
  → supabase.auth.signInWithOAuth({ provider:'kakao', redirectTo: 앱주소 })
  → 카카오 로그인·동의
  → Supabase 콜백  https://swiferlakcaeokzwlgyz.supabase.co/auth/v1/callback
  → 앱으로 복귀 (redirect_to)
```

그래서 설정이 이렇게 갈린다:

- **카카오의 "로그인 리다이렉트 URI"** (플랫폼 키 > REST API 키)
  = Supabase 콜백 주소. **우리 도메인과 무관하니 도메인을 바꿔도 건드리지 않는다**
- **앱으로 돌아올 주소**는 Supabase가 관리한다
  → Supabase > Authentication > URL Configuration 의 **Site URL / Redirect URLs**
  → 새 도메인 추가 시 `https://ra-review.com/**` 형태로 (쿼리 `?view=` 를 달고 돌아오므로 `/**` 필요)

⚠️ `redirect_to` 가 Supabase 허용 목록에 없으면 **Site URL로 되돌린다.**
Site URL이 옛 주소면 엉뚱한 데로 떨어지니 둘 다 맞춰야 한다.

---

## 확인 방법

**로그인이 어디로 돌아오려 하는지** — 브라우저 콘솔에서:

```js
const { data } = await getSupabase().auth.signInWithOAuth({
  provider: 'kakao',
  options: { redirectTo: location.origin + location.pathname, skipBrowserRedirect: true }
});
new URL(data.url).searchParams.get('redirect_to');   // 여기가 새 도메인이어야 한다
```

`skipBrowserRedirect: true` 라 실제로 이동하지 않고 URL만 만들어 볼 수 있다.

**카드 링크 확인** — 카카오링크 카드는 카카오가 관리해서 길게 눌러도 "링크 복사"가 없다.
카드를 눌러 연 뒤 하단 `⋮` → **다른 브라우저로 열기** 로 주소창에서 확인한다.

---

## 겪었던 에러

| 코드 | 원인 / 해결 |
|---|---|
| **KOE205** | 동의항목 문제. Supabase가 `account_email` 을 강제 요청해서 **선택 동의로 열어** 해결 |
| **KOE006** | 등록되지 않은 리다이렉트 URI. 카카오의 로그인 리다이렉트 URI 확인 |
| (에러 없이 404) | 위 3번 웹 도메인 문제. 에러가 안 나서 제일 찾기 어렵다 |

---

## 옛 UI ↔ 새 UI

예전엔 **"앱 설정 > 플랫폼 > Web > 사이트 도메인"** 하나가 SDK 실행과 링크 이동을 **둘 다** 담당했다.
지금은 그게 **둘로 쪼개졌다**:

```text
구: 플랫폼 > Web > 사이트 도메인
      ├→ 신: 플랫폼 키 > JavaScript 키 > JavaScript SDK 도메인   (SDK 실행)
      └→ 신: 제품 링크 관리 > 웹 도메인                          (링크 이동·기본값)
```

옛 문서나 블로그를 보고 "사이트 도메인"만 찾으면 **둘 중 하나만 고치게 된다.** 오늘 그랬다.
