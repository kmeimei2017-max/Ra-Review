# 라리뷰 Ra-Review (구 라봉후기) — 프로젝트 지침

AI 코딩 도구가 이 프로젝트에서 작업할 때 항상 따를 규칙. (작업 전 꼭 읽기)

## 한 줄 소개
바다낚시 후기를 **카톡으로 쉽게 공유**하는 웹앱. 동호회원이 밴드/카톡 단톡방에 후기를 던지는 용도. 현재 테스트 버전.

## ⭐ 정체성 = "0 friction" + 주 전장은 카카오톡 단톡방
- 핵심 = **단순함 / 접근성("나도 할 수 있겠다") / 공유카드로 바로 접근**. 기능은 "더하기"가 아니라 마찰을 "빼기".
- **모든 기능 결정의 나침반: "이게 마찰을 더하나, 빼나?"** → 빼면 채택, 더하면 아무리 멋져도 신중.
- 카톡의 무기 = "이미 모여 매일 떠드는 실시간 대화". 우리 앱 = **"단톡방 대화에 불을 붙이는 연료"**.
- 기능 판단 기준: "이게 단톡방에 후기를 더 쉽고 신나게 던지게 해주나?"

## 화면 용어 (반드시 통일)
| 용어 | 코드 id | 설명 |
|---|---|---|
| **메인페이지** | `homePage` | 후기 목록(홈) |
| **미리보기페이지** | `previewPage` | 앱 안에서 후기를 보는 화면 — 하단 드로어에 **보내기**(항상) + **삭제**(내 후기만, 실질적으로). 수정은 좌스와이프 |
| **편집(작성)페이지** | `writePage` | 후기 쓰는 곳 |
| **후기페이지 (카톡)** | `reviewPage` | 친구가 카톡 링크로 여는 **읽기 전용** 화면. 맨 뒤에 재진입 카드(`4/4`)가 붙는다 |

- 사용자가 **"상세페이지"**라고 하면 그대로 진행하지 말고 **"편집페이지 말씀이신가요?"라고 되물어 확인**.
- **구경 모드** — 남의 후기를 좌스와이프하면 편집페이지가 열리되 아무것도 눌리지 않는다. 무반응으로 막으면 "화면은 3개"라는 안내가 거짓말이 되기 때문.
- **잠긴 후기(`locked`)** — 보내기·삭제 버튼만 숨는다. 좌우 스와이프·편집은 그대로 된다.
- **캡션 `[앱]`·`[카톡]`** — 사진 캡션 **줄머리**에 붙이면 그 화면에서만 보인다(`captionFor()`). 표시 없는 줄은 양쪽 공통이고, 안 쓰면 예전 그대로다. 미리보기엔 보내기 버튼이 있고 후기페이지엔 없어서, "보내기를 눌러요" 같은 안내가 카톡에서는 **없는 버튼을 가리키기** 때문에 만든 장치.

## 누가 무엇을 보나 (열람 범위)
- **후기 하나 = 카톡·밴드에 던지는 한 장의 공유카드.** 앱에 모아두는 게 아니라 내보내는 게 목적.
- **회원 = 각자 자기만의 라리뷰를 갖는다.** 메인페이지엔 내가 쓴 후기만. 가입하는 순간 기본 후기 3개가 내 것으로 복사된다(수정·삭제 자유).
- **비회원** — 메인페이지엔 기본 후기 **1·2·3만**.
- **링크(`?view=<share_token>`)를 받은 사람** — 그 후기 **하나만**. 로그인 필요 없다.

- 🚨 링크에 실리는 값은 후기 id가 아니라 **`share_token`(32자 hex)**이다. id는 알아내면 열리는 이름표라, 예전엔 링크를 안 받은 사람도 전 회원 후기를 통째로 조회할 수 있었다(`009`·`010` 마이그레이션이 고쳤다).
- ⚠️ `anon`은 **비회원이 아니라 앱 공개 키**다. 회원이 로그인해도 이 키를 쓴다(위에 로그인 토큰이 얹힐 뿐). "anon을 막자"를 "비회원을 막자"로 읽으면 위 🚨 규칙을 잘못 건드리게 된다.
- 카드를 받은 사람이 **전달·복사하는 건 막을 수 없고 막아서도 안 된다** — 단톡방에 퍼뜨리는 게 이 앱이 하려는 일이다.
- 사진 파일(Storage)은 여전히 public. URL을 알면 열리지만 URL을 알려면 후기를 조회해야 한다. 완전히 잠그려면 signed URL이 필요하고 그러면 **카톡 미리보기 이미지가 깨져서** 하지 않는다.
- **관리자의 "남의 후기 삭제"는 앱에 경로가 없다.** 삭제 버튼은 미리보기페이지에만 있고 거기엔 내 후기만 뜬다(개인 피드 전환 이후). 남의 후기 삭제는 **Supabase 대시보드에서** 한다. `canDeleteTrip()`의 `isAdmin()`과 RLS의 `is_admin()`은 그대로 두되(미래에 화면이 생기면 필요), 지금은 쓰이지 않는다.

## UX 원칙 (확정된 결정)
- 사진은 **한 장씩 스와이프**하며 읽기(보는 사람이 페이스 주도). **슬라이드쇼·배경음악·영상 자동생성 안 함**.
- UI 효과는 **잔잔하게**(호수 물방울 번짐처럼 느리고 부드럽게) — 급한 느낌 금지.
- 후기 방향(가로/세로)은 **섞지 않고** 첫 사진 기준 자동 감지로 통일.
- 안내문구는 최소화. 직관적 UX 선호.

## 기술 스택 / 메모
- **단일 `index.html`** (HTML/CSS/JS 한 파일).
- DB·저장: **Supabase** (Postgres + Storage 버킷 `trip-photos`). ⚠️ 한글/특수문자 불가 → 업로드 파일명은 **확장자만**(`${Date.now()}_${i}${ext}`).
- 🚨 **`trip_by_token()` RPC의 anon 실행 권한은 절대 막지 말 것.** 카톡으로 받은 링크를 여는 **유일한 통로**다. 앱(`loadTripByToken()`)과 OG 워커(`cloudflare-worker/og.js`)가 둘 다 이걸로 조회한다. 막으면 카톡 미리보기 + 공유 열람이 동시에 죽는다 = 이 앱의 존재 이유가 죽는다.
  - `security definer`라 RLS를 우회한다. 그래서 목록을 좁혀도 토큰을 가진 사람은 그 후기 하나를 계속 본다. service_role 키는 쓰지 않는다(관리할 비밀을 늘리지 않으려고).
  - 예전 규칙("`trips`·`photos`의 anon SELECT를 절대 막지 말 것")은 **폐기됨**. 워커가 단순 SELECT에 기대던 시절의 규칙이고, 그 때문에 전 회원 후기가 통째로 조회됐다(`010_narrow_select.sql`이 좁혔다).
- ⚠️ RLS 거부는 **에러가 아니라 200 + 0행**으로 돌아온다. 삭제/수정 성공 판정은 `error`만 보면 안 되고 실제 반영된 행 수를 확인해야 한다.
- ⚠️ 후기 삭제 순서 **Storage → photos → trips** 고정. Storage 정책이 `trips` 행으로 소유자를 판정하므로 순서를 바꾸면 사진이 고아로 남는다.
- 카톡 동적 미리보기(OG): **Cloudflare Worker** (Supabase는 text/plain 강제라 불가).
- 카톡 공유: **카카오링크(Kakao JS SDK) 원터치**. 밴드/일반링크용 OG는 Worker 유지.
- 🚨 **카카오 디벨로퍼스 설정은 [KAKAO.md](KAKAO.md) 를 먼저 볼 것.** 콘솔에 **도메인 설정이 세 군데**(앱 대표 도메인 / JS SDK 도메인 / **제품 링크 관리 > 웹 도메인**) 있고 하는 일이 전부 다르다. 특히 **카톡 공유 링크의 이동 허용·기본값은 "제품 링크 관리 > 웹 도메인"** 이다 — 여기가 옛 도메인이면 카드 링크가 **기본 웹 도메인(경로 없는 루트)으로 갈아치워져** 404가 난다. 에러가 안 나서 찾기 가장 어렵다.
- 배포: **GitHub Pages** (repo `kmeimei2017-max/Ra-Review`).
- 앱 이름은 **라리뷰(Ra-Review)로 통일** 결정됨(도메인 구매 예정). 기존 "라봉후기" 표기는 순차적으로 교체.

## 🧠 개발 원칙 (Karpathy 원칙)
1. **생각 우선**: 구현 전 계획을 짧게 설명. 불확실하면 추측 대신 질문.
2. **단순성 유지**: 요청된 기능에 딱 필요한 최소 코드만.
3. **최소 수정**: 요청받은 파일·기능만 수정. 요청 안 한 주변 정리 금지.
4. **목표 기반 검증**: 완료 선언 전 브라우저에서 `index.html`을 직접 열어 실제 동작 확인.

## 작업 규칙
- **커밋/푸시는 사용자가 명시적으로 요청할 때만**.
- 백업 파일은 **`index_backup_NNN.html`** (번호만, 한글 설명 X).
- 커밋 후 모바일 확인용으로 `?v=` 캐시버전 안내.
- 커밋하지 말 것: `.claude/`, 원본 카카오톡 사진, 데모/테스트 파일.

## 응답 규칙 (대화)
- **한국어**로 답변.
- 매 답변 **맨 끝에 주황 구분선** 한 줄: `🟠━━━━━━━━━━━━━━━━━━━━━`
