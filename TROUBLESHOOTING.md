# 문제 해결 기록

겪은 문제와 그 원인을 남겨두는 곳. **같은 자리를 다시 건드리기 전에 여기부터 읽을 것.**

---

## 후기칸을 누르면 다음 사진이 딸려 오던 것

**2026-08-13** · 상태: **해결 완료**(갤럭시·아이폰 실기기 확인) · 🚨 **이 자리는 열 번 넘게 팠다**

> **이 절이 있는 이유** — 원인이 늘 같은 자리였는데 기록이 커밋 메시지에만 있어서 매번 처음부터 다시 알아냈다. **편집화면의 사진·후기칸·스크롤을 건드리기 전에 반드시 읽을 것.**

### 뿌리 원인 (한 줄)

**키보드가 올라올 때 브라우저가 스스로 커서를 화면 한가운데로 스크롤한다.**

우리가 아무리 잘 맞춰놔도 **그게 우리보다 늦게 일어나면 통째로 덮인다.** 카드가 위로 밀리고, 그 아래 빈자리로 다음 사진이 들어온다.

### 증상이 조금씩 달라 보였던 이유

같은 원인인데 **어느 칸에서 키보드가 처음 올라오느냐**에 따라 다르게 보인다.

| 언제 | 보이는 것 |
|---|---|
| **키보드가 없는 상태에서 처음 누른 칸** | 다음 사진이 딸려 온다 ← 문제 |
| 키보드가 이미 올라온 뒤 누른 칸 | 멀쩡하다 (브라우저가 스크롤할 일이 없다) |
| 예전 기록: "마지막 사진만 멀쩡했다" | 마지막 카드는 아래에 딸려 올 것이 없어서 |

**"사진1에서만 그런다"는 말은 사진1이 특별해서가 아니라, 거기서 키보드가 처음 올라오기 때문이다.** 사진2를 먼저 누르면 사진2에서 난다.

### 지금까지의 시도와, 각각을 왜 버렸는지

| 커밋 | 방법 | 결과 |
|---|---|---|
| `5f9a9b2` | 카드 아래 **빈 여백**을 깔아 다음 사진을 밀어냄 | **버림** — 검은 여백이 화면에 남는다 |
| `c4c019a` | 키보드만큼 앱을 줄이고(`--avail`) **카드 윗변을 화면 맨 위에** | **버림** — 카드 높이가 급변하면 스크롤이 범위를 벗어나 아이폰에서 화면이 통째로 검게 나왔다 |
| `fdfd572` | 사진 높이를 계산하지 말고 **재서** 쓰기 | **유지** |
| `b4dc269` | 기준을 카드 윗변 → **입력칸**(`scrollIntoView({block:'end'})`) | **유지** — 아이폰 검은 화면을 고쳤고, 지금은 이 한 줄이 다음 사진까지 막는다 |
| `79bd89d` | 브라우저의 가운데 정렬을 **뒤늦게 되맞추기** | **유지(안전망)** — 이것만으로는 과도기가 눈에 보였다 |
| `94bf188` | 키보드 자리를 **미리 비우기**(`--avail` 선축소) | **버림** — 한 프레임에 확 줄어 번쩍였다 |
| `8934713`·`1ba2f51` | 사진 크기를 **하나로 통일**(CSS 변수 `--pf-photo-h`) | **유지** |
| `0080218` | 카드 높이 규칙 **삭제** | **유지** — 아래 원칙 ①②의 핵심 |
| `e2dcb4c` | `kbHeight` 를 **멈춘 뒤에만** 학습 | **유지** — "번쩍"의 진짜 원인이었다 |
| `b6f9462`·`71e4c5b` | 상단바 접기를 **키보드 시점으로** / 내려갈 땐 **따라다니지 않기** | **유지** |

### 지금 방식 — 원칙 두 줄

> **① 다음 사진을 막는 건 CSS 규칙이 아니라 정렬 한 줄이다.**
> `input.scrollIntoView({ block: 'end' })` — 입력칸 밑변이 화면 밑변이면 그 뒤에 오는 것(카드의 남은 부분도, 다음 카드도)은 **정의상 화면 밖**이다. 그래서 카드 높이를 못 박던 CSS 규칙은 **지웠다**.
>
> **② 화면 크기를 따라 변하는 것이 하나도 없어야 한다.**
> 키보드가 오르내리는 동안 화면 높이는 수십 번 바뀐다. 그때마다 같이 변하는 게 있으면 그만큼 화면이 움직인다. 사진 크기(`--pf-photo-h`)도 카드 높이도 **키보드가 올라온 뒤의 화면 높이(`kbHeight`) 기준 고정값 하나**로 끝낸다.

구체적으로는

1. **키보드가 올라온 뒤의 화면 높이를 기억한다** (`syncAvail()` → `rabong_kb_h`). `localStorage` 에 두는 건 앱을 새로 연 직후 누르는 **첫 후기칸까지** 덮기 위해서다
2. **사진 크기는 CSS 변수 하나**(`--pf-photo-h`, `setPhotoHeightVar()`). 쓸 때든 볼 때든 같다
3. **누르는 순간에는 아무것도 하지 않는다.** 상단바 접기·정렬을 `focusin` 에서 하면 키보드보다 먼저 화면이 움직인다 → 키보드가 올라오는 순간(`visualViewport resize`)으로 미뤄 **애니메이션에 묻는다**
4. **내려가는 중에는 따라다니지 않는다.** 사용자는 나가는 중이다
5. `realignWhenSettled()` 는 **안전망**으로 남긴다 — 브라우저가 스스로 커서를 화면 한가운데로 옮기는 경우 대비

### 🚨 하지 말 것

- **빈 여백(padding)으로 밀어내기** — 검은 여백이 화면에 남는다 (`5f9a9b2` 에서 겪음)
- **`visibility`·`opacity` 로 다음 카드 숨기기** — 다음 후기칸을 한 번에 못 누른다
- **카드 높이를 `calc(var(--avail) - 18px)` 로 되돌리기** — `--avail` 은 키보드가 올라오는 **동안** 계속 바뀐다. 그때마다 카드가 줄어 입력칸이 위로 밀리고 아래에 틈이 벌어져 다음 사진이 비친다. 위 원칙 ①에 따라 **필요도 없다**
- **애니메이션 중간값을 최종값으로 배우기** — `kbHeight` 를 `resize` 마다 곧바로 받으면 중간값(예: 223)이 박혀 사진 높이가 `223−18−117−22 = 66px` 로 **토막 난다**. 화면이 100ms 남짓 통째로 검게 나왔다. **멈춘 뒤 300ms 지나서만 확정할 것**
- **고정 `setTimeout` 에 기대기** — 키보드 애니메이션이 그보다 길면 진다. 여러 번 이걸로 실패했다
- **사진 높이를 요소마다 `style.maxHeight` 로 박기** — `innerHTML` 로 다시 그릴 때마다 한 프레임은 기본값으로 그려졌다 줄어든다(사진 추가 때마다 반복). **CSS 변수로 둘 것**
- **사진 높이를 "화면 − 후기칸 몇 px" 로 미리 계산하기** — 후기칸 높이는 상황마다 다르다. 재서 넣을 것
- 🚨 **브라우저 주소창이 만드는 움직임을 코드로 쫓기** — 사파리·크롬은 주소창이 키보드에 맞춰 오르내려서 화면 높이가 **키보드와 별개로 한 번 더** 바뀐다. **주소창 위치도 키보드 속도도 웹에서 제어할 수 없다.** 홈 화면 앱(standalone)에는 주소창이 없어 이 문제가 통째로 사라진다 — 그래서 **편집은 홈 화면 앱으로 유도**한다(CLAUDE.md UX 원칙)

### 막히면 — 추측하지 말고 재라

**스크린샷만 보고 짚었다가 두 번 틀렸다.** 순서가 있다. 자세한 명령은 [CLAUDE.md](CLAUDE.md) "실기기 문제 잡는 법".

1. **`?dbg=1` 계측기로 숫자를 띄운다** — 원본은 커밋 `a68b5cf`. `--avail` / 카드 높이 / `gap`(입력칸 밑변↔화면 밑변) / `:focus-within` 매칭 여부. **크기가 틀렸는지 정렬이 틀렸는지** 한 번에 갈린다
2. **0.3초보다 짧은 순간은 캡처로 못 잡는다 → 화면 녹화**
3. **`ffmpeg` 으로 프레임을 뜯는다** — 50ms 간격이면 원인이 보인다

실제로 이 순서로 두 번 결정적이었다.

- 계측: "카드 높이 규칙이 안 걸렸다"는 내 짐작이 **틀렸다.** 카드도 `:focus-within` 도 정상이었고 **스크롤 하나만** 어긋나 있었다
- 녹화: 사진이 "작아진" 게 아니라 **1/4 토막**이 났다는 걸 100ms 단위로 확인 → `kbHeight` 오염이 범인으로 확정

---

## 카톡 인앱 브라우저에서 사진 아래 검은 틈이 생기던 것

**2026-08-12** · 상태: **해결 완료** (커밋 `60b8a4e`)

> **실기기 확인 결과** — 카톡 인앱에서 검은 틈이 사라졌고, **가장 걱정했던 사파리 출렁임도 없었다.** 갤럭시도 이상 없음. 세 환경 모두 통과.

### 증상

카톡으로 받은 후기를 열어 사진을 넘기면, **사진과 화면 아래 툴바 사이에 검은 띠**가 보였다.

| 환경 | 결과 |
|---|---|
| 아이폰 **카톡 인앱 브라우저** | **검은 틈 있음** ← 문제 |
| 아이폰 사파리 | 정상 (사진이 꽉 참) |
| 갤럭시 | 정상 |
| 홈 화면 앱 | 정상 |

**같은 후기, 같은 사진인데 사파리에서는 꽉 차고 인앱에서만 틈이 생겼다.**

### 가로 사진 여백과 헷갈리지 말 것

`.slide-image.landscape { object-fit: contain }` 규칙이 있어서 **가로 사진은 위아래 검은 여백이 원래 생긴다**(좌우를 다 보여주려고 일부러 그렇게 뒀다). 겉모습이 비슷해서 헷갈리기 쉽다.

**구별법** — 같은 사진을 **사파리로도 열어본다.**
- 사파리에서도 여백이 있다 → **가로 사진이라 정상**
- 사파리에서는 꽉 찬다 → **이번 문제(높이 계산 오류)**

### 원인

화면 높이가 `.app` 한 곳에서 아래로 내려간다.

```
.app         height: 100vh
 └ .page      height: 100%
    └ .detail-body   height: 100%
       └ .photo-slide  height: 100%   ← 사진 한 장이 차지하는 칸
```

문제는 **`100vh` 가 브라우저 UI(주소창·툴바)를 빼지 않은 높이**라는 점이다.

- **카톡 인앱 브라우저**는 주소창과 툴바가 **항상 고정**이다. 그래서 `100vh` 로 잡은 사진 칸이 실제로 보이는 영역과 어긋나고, 칸이 화면보다 작아지면서 **그 아래로 다음 사진 칸의 배경**(`.photo-slide { background: var(--bg-card) }`)이 드러났다. 그게 검은 띠의 정체다
- **사파리·갤럭시**는 스크롤하면 툴바가 접히고, `100vh` 를 그 접힌 상태(가장 큰 높이) 기준으로 잡는다. 그래서 사진이 화면을 꽉 채웠다
- **홈 화면 앱**은 `lockAppHeight()` 가 실제 높이를 인라인으로 박아 넣어서 `100vh` 를 아예 쓰지 않는다. 다만 이 함수는 `isStandaloneApp()` 일 때만 도는 탓에 **인앱 브라우저는 보호받지 못했다**

### 고친 방법

`index.html` 의 `.app` 에 `dvh` 를 덧붙였다.

```css
height: 100vh;    /* dvh 를 모르는 브라우저용 폴백 */
height: 100dvh;   /* 브라우저 UI를 뺀, 지금 실제로 보이는 높이 */
```

같은 속성을 두 번 쓰는 건 **폴백 기법**이다. `dvh` 를 모르는 브라우저는 뒷줄을 무시하고 앞줄을 쓰고, 아는 브라우저는 뒷줄로 덮어쓴다.

### 다시 건드릴 때 주의

**`svh` 로 바꾸지 말 것.** `svh` 는 "툴바가 최대로 펼쳐진 상태" 기준이라, 툴바가 접히는 사파리에서는 오히려 아래에 여백이 생긴다. **지금 멀쩡한 쪽을 망가뜨린다.**

**`--avail` 을 `.app` 에 걸지 말 것.** 코드 주석에도 금지로 적혀 있다 — 주소창이 접힐 때마다 값이 바뀌어서 **메인·미리보기 화면이 스크롤할 때마다 출렁인다.** `--avail` 은 편집화면(`.write-trip`) 전용이다.

### 확인할 것

1. **아이폰 카톡 인앱** — 후기 2번째 사진에서 툴바와의 검은 틈이 사라졌는지
2. **아이폰 사파리** — 위아래로 스크롤할 때 **화면이 출렁이지 않는지** ← `dvh` 는 주소창이 접히고 펼쳐질 때 값이 변한다. 출렁이면 이 방법은 실패다
3. **갤럭시 / 홈 화면 앱** — 평소와 같은지

**실패했을 때의 대안** — `dvh` 를 되돌리고, `lockAppHeight()` 의 `isStandaloneApp()` 제한을 풀어 **인앱 브라우저에서도 실측 높이를 인라인으로 박는** 방향을 검토한다.
