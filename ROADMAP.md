# 라리뷰 Ra-Review (구 라봉후기) — 로드맵

할 일과 끝난 일. 완료되면 `할 일` → `완료`로 옮긴다.

## 할 일

### 지금 — 첫인상 설계

"처음 만난 사람에게 뭐라고 말할까"의 문제다. 노려야 할 감정은
**"멋있다"가 아니라 "나도 쉽게 하겠다".**
후기를 다 본 사람은 이미 부러운 상태라 동기는 충분하다. 그다음에 오는
*"근데 나는 저렇게 못 쓸 것 같은데"* 만 지워주면 된다.
(같은 덩어리였던 **재진입 카드(`4/4`)는 완료** — 아래 `완료` 참고)

- [ ] **기본 후기 3개 내용 채우기** — 배관은 완료, **내용이 아직 테스트용**
  - 제목이 **번호로 시작한다.** 화면에 보이는 순서와 같아서 어느 후기인지 헷갈리지 않는다:

    | # | 후기 | 제목 | 잠금 |
    | --- | --- | --- | --- |
    | 1 | 테스트 | `1.여기부터~` | ❌ |
    | 2 | 설명서 | `2.세로로~` | 🔒 |
    | 3 | 라리뷰 | `3.Ra-Review 개발 동기` | 🔒 |

  - **1. 테스트 후기** — "마음에 안 들면 지우면 그만"을 실제로 해보게 하는 용도
    - **잠그지 않는다.** 지워보는 게 이 후기의 존재 이유다
  - **2. 설명서 후기** — **9:16 세로 촬영 안내를 여기 넣는다**
    (찍기 전 안내창·별도 설명서 화면은 안 만들기로 함 → `검토 후 안 하기로 한 것`)
    - **잠금 대상** — 보내기·삭제 버튼만 숨고, **편집은 좌스와이프로 그대로 된다**
  - **3. 라리뷰 후기** — 개발 동기 · 같이 만들자
    - **잠금 대상** (설명서와 같은 규칙). `008_lock_ra_review.sql` 실행 완료 — 원본 2건 모두 `locked`
  - 순서는 **테스트 → 설명서 → 라리뷰** 로 정함 (`006` 실행 완료)
    - 바꾸고 싶으면 원하는 순서대로 나열만 하면 된다:
      `select admin.order_templates('설명서', '테스트', '라리뷰');`
    - 제목 일부만 써도 되고, 애매하면 후보를 보여주며 예외가 난다
  - **제목·사진·글 수정은 앱에서 하면 된다.** 슈파베이스가 필요 없다 —
    `is_template`·`locked` 표시는 후기의 id에 붙어 있어서 제목을 바꿔도 유지된다.
    **잠긴 후기(2·3)도 앱에서 고칠 수 있다** — 좌스와이프로 편집페이지에 들어간다.
    순서만 위 함수가 필요하다 (앱에는 작성 시각을 바꾸는 화면이 없다)
  - 지금은 회원이 관리자 혼자라 **몇 번을 고쳐 써도 아무 영향이 없다.**
    첫 회원이 가입하는 순간의 내용만 맞으면 된다
  - 앱에서 수정하면 **그 이후 가입자부터** 반영된다 (기존 회원 것은 이미 자기 소유라 안 바뀜)

### 그 다음

- [ ] **신규 가입 실검증** — 다른 카카오 계정으로 가입해 기본 후기 3개가 복사되는지 확인
  - `select * from admin.members;` 에서 `seeded_count`가 3이면 성공
  - 이어서 그 계정으로 기본 후기 하나를 **삭제** → 관리자 계정의 원본 사진이 그대로인지 확인
    (사진 URL 공유 방식이 안전한지 보는 마지막 관문)
- [ ] **메인/미리보기/편집 마무리** (세로고정 — 카톡 메인)
- [ ] **영상 업로드 거부 메시지 개선**
  - 지금은 "영상이 너무 커요 (90MB)"만 뜨고 어떻게 하라는 말이 없다 → "카메라 화질을 1080p로 낮춰서 다시 찍어주세요"
  - 용량 검사가 길이 검사보다 **먼저** 실행돼서, 20초짜리 4K 영상에 "너무 길어요"가 아니라 "너무 커요"가 뜬다 → 순서 뒤집기
  - 제한 숫자(1개·15초·50MB)는 그대로 둔다
- [ ] **앱 이름 `라봉후기` → `라리뷰(Ra-Review)` 로 교체** — **결정 완료**(도메인 구매 예정), 실행만 남음
  - 카카오 디벨로퍼스: `내 애플리케이션 > [앱] > [일반] > [앱 기본 정보] > 앱 이름` — 대시보드 설정이라 코드 변경 없음
    - ⚠️ 이 이름은 공유카드 출처 줄뿐 아니라 **카카오 로그인 동의 화면에도 같이 표시된다**
  - **`index.html`은 이미 다 교체됐다**(2026-08-03 확인 — "라봉후기" 0건). 남은 화면 노출은
    [manifest.webmanifest:3](manifest.webmanifest#L3) `short_name` 하나뿐 (홈화면 아이콘 이름)
    - 워커·SQL 파일 첫 줄 주석에도 남아 있지만 **화면에 안 보이므로 급하지 않다**
  - 문서상 이름 변경에 별도 심사 안내는 없었으나 비즈앱이라 실제로는 다를 수 있다

### 나중에

⚠️ 표시가 붙은 것은 **나침반("마찰을 더하나, 빼나?")에 걸린 항목**이다. 손대기 전에 그것부터 답할 것.

- [ ] **묵호방 같은 후기방 고려**
  - ⚠️ 메인은 이미 **개인 피드**로 갔다(`완료` 참고). 방을 만들면 "어느 방에 쓸지" 고르는 마찰이 새로 생긴다.
    되살리려면 **방이 마찰보다 크게 주는 게 뭔지** 먼저 적을 것
- [ ] **좌우 유도 모션 아이콘**
  - 넛지/바운스, 그 밖의 아이디어
  - 메인화면에서 떨어지는 구슬 — 일부는 메인에, 일부는 바닥에 튕겨 우측하단·좌측상단으로 애니메이션
    (원래 "4개"라고 적혀 있었지만 뒤에 나열한 개수와 안 맞아 숫자는 뺐다. 만들 때 정할 것)
  - ⚠️ CLAUDE.md **"UI 효과는 잔잔하게 — 급한 느낌 금지"** 와 부딪힌다. 튕기는 모션이면 특히
  - ⚠️ ~~카카오 회원가입 후 온보딩 1, 2번째 후기 안내~~ — **설명서 후기가 이미 그 역할**이고,
    "안내문구 최소화" 원칙과도 겹친다. 별도 온보딩을 다시 꺼낼 이유가 생기면 그때 적을 것
- [ ] **가로본능 (추후 연구)** 지금은 세로본능(세로 중심 UX)에 집중

## 완료

- [x] **공유카드 → 재진입 카드** (후기 마지막에 `4/4`) — [index.html:1553-1600](index.html#L1553-L1600), [index.html:1806](index.html#L1806) `enterAppFromReview()`
  - 노림수: 단톡방에서 공유카드를 보고 **"나도 써봐야겠네"** 하게 만드는 것. 읽는 사람을 쓰는 사람으로 바꾸는 고리다
  - **후기페이지(`reviewPage`)에만 붙는다.** 내 피드에서 보는 미리보기페이지는 그대로 `3/3`
  - ⭐ **슬라이드 번호에 포함시킨다** (사진 3장이면 마지막 카드가 `4/4`). `3/4`가 보이면 한 장 남은 걸
    알고 자연스럽게 넘어온다 — 광고를 들이미는 게 아니라 후기의 마지막 장이 된다
  - ⭐ **"가입"·"회원"이라는 말을 쓰지 않는다.** 가입은 일이고 쓰기는 욕구다
  - ⭐ **모든 후기에 공통으로 붙는다.** 특정 후기를 구분하는 코드가 없다(구분하면 하드코딩이 생겨
    나중에 후기를 고칠 때 깨진다). "안내(설명)"는 사진 캡션이 담당한다
  - **로그인 여부와 무관하게 항상 붙되 문구만 갈린다** — 후기페이지의 유일한 출구라 회원에게도 필요하다.
    다만 회원에게 "후기 하나 완성돼요"를 매번 권하면 잔소리라 피드백 요청으로 바꿨다:

    ```text
    [비회원]  여기까지 보셨네요 🎣 / 사진 몇 장이면 후기 하나 완성돼요
    [회원]    같이 만들어가는 앱이에요 / 불편한 점이나 아쉬운 점 편하게 알려주세요
    [공통 버튼]  Ra-Review 열기
    ```

  - 버튼은 `goToHome()`만 한다. **작성화면을 강제로 열지 않는다** — 쓰기로 마음먹은 사람은
    [index.html:1335](index.html#L1335) `WRITE_INTENT_KEY` 배관이 알아서 받는다
  - 안 하기로 한 것: 화면에 항상 떠 있는 버튼(후기 감상을 방해, 광고처럼 보임) / 공유카드에 버튼 2개
    (아직 후기를 안 본 사람에겐 동기가 없음) / 버튼 문구를 "나도 후기 쓰기"처럼 행동을 못박는 말로 하기(강요하는 느낌)
- [x] **남의 후기는 "구경 모드"로 열린다** — 좌스와이프하면 편집페이지가 뜨되 아무것도 눌리지 않는다
  ([index.html:1811](index.html#L1811))
  - 조용히 무시하면 좌스와이프가 무반응이라 **"화면은 3개"라는 안내가 거짓말이 된다**
  - 로그인을 바로 띄우지 않는 이유: 실수로 스친 스와이프가 화면을 날리면 안 된다
  - 🚨 **막는 건 `.write-content`의 자식들이지 컨테이너 자신이 아니다**
    ([index.html:708-716](index.html#L708-L716)). 컨테이너가 곧 스크롤 영역이라 거기에
    `pointer-events:none`을 걸면 **상하 스와이프가 통째로 죽는다** — 실제로 그랬고
    첫 화면 18%(4465px 중 822px)만 보였다
  - 🚨 **우스와이프도 살아 있어야 한다.** 구경 모드는 `editingTripId`가 `null`이라
    `goToPreviewFromWrite()`가 그냥 `return` 하면 무반응이 된다
    ([index.html:2348](index.html#L2348)에서 `trips[currentTripIndex]`로 되돌아간다).
    배너가 **"좌우로 밀어 더 구경하셔도 돼요"** 라고 말하므로 한쪽만 되면 안 된다
- [x] **잠긴 후기(`trips.locked`) — 첫 사용자를 위한 안내 후기만 예외**
  - 대상: **2.설명서**, **3.라리뷰** 둘뿐. **1.테스트는 잠그지 않는다** —
    "마음에 안 들면 지우면 그만"을 실제로 해보는 게 그 후기의 용도다
  - **잠금이 숨기는 건 보내기·삭제 버튼(드로어)뿐이다.** 안내 후기 사진 안에 이미
    버튼 그림이 들어있어서 진짜 버튼이 그 위에 겹쳐 보이기 때문
  - ⭐ **좌우 스와이프는 잠긴 후기에서도 그대로 된다** (좌 → 편집, 우 → 홈).
    드로어가 통째로 숨는 화면이라 이동까지 막으면 좌스와이프가 무반응이 되고,
    안내 문구를 앱에서 고칠 방법도 사라진다
  - **삭제만 계속 막는다**(`canDeleteTrip`). 실수로 지우면 다시 볼 방법이 없다
  - 잠금은 후기 `id`에 붙어서 **제목·내용을 고쳐도 유지된다**
  - 관리: `select admin.set_locked('제목 일부');` / 풀기 `select admin.set_locked('제목 일부', false);`
    - ⚠️ 템플릿 원본과 "가입 때 받은 복사본"은 **제목이 같은 별개의 행**이라 위 함수가 예외를 낸다.
      둘을 한 번에 잠그려면 `008_lock_ra_review.sql` 처럼 **제목 앞번호**(`3.`)로 `update` 한다
- [x] **메인 ↔ 미리보기 ↔ 편집 좌우 스와이프 순환 이동**
  - 🚨 **비회원도 여섯 방향이 전부 뚫려 있어야 한다.** 순환을 한 칸이라도 막으면 거기서 끊긴다 —
    `resumeEditTrip()`이 비회원을 `return` 시키던 탓에 **메인→우스와이프만 무반응**이었다
    (미리보기→좌스와이프는 구경 모드로 열리는데 같은 화면으로 가는 다른 길만 막혀 있었다).
    지금은 열 후기만 고르고 **편집 가능 여부는 `goToEditTrip()`이 판정**한다
  - 메인에서 스와이프하면 **보던 후기로 이어서 돌아간다**(`lastViewedTripId`).
    항상 최신 후기로 튕기면 2번·3번 후기는 탭으로만 열 수 있어 좌우 이동이 거기서 끊겼다
  - 처음 들어왔거나 보던 후기가 삭제됐으면 **가장 최근 후기**를 연다 (기존 규칙 그대로)
- [x] **카카오 로그인** 회원가입
  - 1단계: 로그인/로그아웃 (비즈앱 전환 완료, KOE205 해결 — Supabase가 account_email을 강제 요청해서 선택 동의로 열어둠)
  - 2단계: `trips.author_id` + RLS (읽기 전체 공개 / 쓰기·수정·삭제는 본인만), 기존 후기는 관리자 소유로
  - 3단계: `profiles` 테이블 (닉네임·프로필사진·is_admin·차단여부)
  - 확정된 규칙: 작성=로그인 필수(열람은 비로그인 유지) / 관리자=남의 후기 **삭제만**(수정 불가) / 차단=**쓰기 전면 중지**(열람·기존 후기는 유지) / 닉네임·사진=카카오 것 트리거 자동 동기화
  - 클라이언트(index.html) 수정 + SQL 3개(`supabase/migrations/001~003`) 대시보드 실행까지 완료
- [x] **Supabase 관리자 대시보드 및 회원관리**
  - Supabase 기본 대시보드 활용 (앱 안에 관리자 화면은 안 만듦)
  - `supabase/migrations/004_admin_views.sql` 작성 + 대시보드 실행 완료
  - 사용법: `select * from admin.members;` / `select * from admin.reviews;` / `select admin.block('닉네임');` / `select admin.grant_admin('닉네임');`
  - 닉네임에 공백·특수문자가 있으면 뷰에서 뽑아 넘기는 게 안전:
    `select admin.block(id::text) from admin.members where nickname like '%이름%';`
  - `admin` 스키마는 PostgREST 미노출 → anon 키로 도달 불가(404 확인). 기존 테이블·RLS는 안 건드려서 앱 영향 0
  - ⚠️ 차단/해제 후 앱은 **새로고침해야 반영**된다. `myProfile`을 로그인 시 1회만 읽기 때문
- [x] **개인 피드 전환 + 가입 시 기본 후기 3개 자동 지급** (`005_signup_templates.sql` 실행 완료)
  - 메인화면이 **내 후기만** 보여준다. 로그인 전에는 기본 후기(`is_template`)를 보여준다
  - 가입하는 순간 템플릿 후기가 그 회원 것으로 복사된다 (수정·삭제 자유)
  - 🚨 **사진 파일은 복사하지 않고 원본 URL을 재사용한다.** `deleteTrip()`이 "후기 폴더 기준"으로
    지우기 때문에 안전하다. 이걸 **"URL 기준"으로 바꾸면 회원 전원의 사진이 한꺼번에 깨진다**
  - 🚨 카톡 링크(`?view=`)는 피드에 없는 후기라 `loadTripById()`로 따로 가져온다.
    **이 경로를 없애면 공유가 통째로 죽는다**
  - 관리자: `select admin.set_template('후기 제목');` / `admin.members`의 후기 수는 이제
    **직접 쓴 것만** 세고 `seeded_count`가 따로 나온다
  - 폰에서 확인 완료 — 로그인 피드 / 카톡 공유 링크 / 로그아웃 시 기본 후기 전환 모두 정상
  - 신규 가입 시 실제 복사는 아직 미검증 → `할 일 > 그 다음`의 **신규 가입 실검증** 참고
- [x] **로그인 프로필 버튼을 우하단 FAB(Floating Action Button) 스택 맨 아래로** (70px 흰 원, 나머지 3개는 한 칸씩 위로)
- [x] **편집페이지 사진/영상 안내문구 2줄로 정리** (크기·굵기는 "제목" 라벨과 동일)
- [x] **버그: 편집에서 사진을 빼도 저장되지 않던 문제**
  - `removePhoto()`가 화면 배열에서만 빼고 `persistTrip()`엔 삭제 명령이 없어서, 지운 사진이 새로고침하면 되살아났다
  - 지운 사진의 id를 `removedPhotoIds`에 기억해뒀다가 저장 시 `photos` 테이블에서만 삭제하도록 수정 (Storage 파일은 그대로 둠 — 기본 후기 복사본들이 URL을 공유하므로)

## 검토 후 안 하기로 한 것

다시 논의하지 않도록 결론만 남긴다.

- **앱 안에 자체 카메라(`getUserMedia`) 만들기** — 9:16을 강제할 수 있고 영상 용량 문제도 해결되지만, 폰 기본 카메라의 화질(HDR·야간모드·손떨림보정)을 포기해야 한다. 낚시 후기는 새벽·해질녘 사진이 많아 손해가 크다.
- **찍은 사진을 편집페이지로 자동 전달** — 사용자는 여러 장을 갤러리에 모아두고 구상하면서 고른다. 바로 끌고 가는 게 오히려 마찰이다. **갤러리 저장 방식 유지.**
- **업로드할 때 자동 9:16 크롭** — 사람 머리나 물고기 꼬리가 잘린다.
- **찍기 전 9:16 안내창** — 카메라 버튼을 누를 때마다 안내를 띄우는 것. 몇 번 써보면 알게 되는 걸
  매번 띄우면 마찰만 는다. 대신 **기본 후기 2번(설명서)에 넣는다.**
- **별도 사용설명서 화면** — 기본 후기 3개가 그 역할을 한다. 설명서를 후기 형태로 주면
  넘겨보면서 배우는 동시에 "후기가 이렇게 생겼구나"를 눈으로 익힌다.
- **앱 안 관리자 화면** — Supabase 대시보드로 충분하다 (`admin` 스키마).
- **공유카드 출처 영역(`🔘 앱이름` 줄)을 앱 진입로로 쓰기** — 카카오가 정한 대로 두기로 했다.
  **4/4 카드와 기본 후기 3개로 승부한다.**
  - 아이콘·이름은 **그림/글자 교체만** 가능하고 **크기·레이아웃은 카카오톡 UI 고정**이라 못 바꾼다
  - **링크 URL은 바꿀 수 있지만 안 한다.** 그러려면 지금의 `Kakao.Share.sendDefault()`
    ([index.html:2449](index.html#L2449))에서 `sendCustom()` + 대시보드 템플릿 등록으로 갈아타야 하고,
    그러면 **카드 디자인이 코드에서 대시보드로 이사한다** (제목·첫 사진·첫 글 자동 조립을 잃는다)
  - 무엇보다 **자리가 나쁘다.** 출처 줄은 카드를 열기도 전이라 아직 아무 감흥이 없다.
    `4/4` 카드는 **후기를 다 본 직후** — 동기가 최고조인 지점이다
  - 참고: 보자기카드(모바일 청첩장)가 이 방식을 쓴다(출처 줄 → 서비스 홈).
    노림수는 우리와 같고 수단만 다르다

## 참고

- 배포 주소 (아래 코드블록에 마우스 올리면 복사 아이콘):

  ```text
  https://kmeimei2017-max.github.io/Ra-Review/
  ```

  - 모바일에서 최신 화면을 볼 땐 뒤에 `?v=숫자`를 붙인다 (캐시 우회)
- 로컬 확인: `npx http-server -p 8765 -c-1` 실행 후 <http://127.0.0.1:8765/index.html>
- 더미 이미지:

  ```text
  https://picsum.photos
  https://unsplash.com/s/photos/cozy-cafe
  ```
